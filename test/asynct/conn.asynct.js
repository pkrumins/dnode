
var Conn = require('../../lib/conn');
/*
Conn
	end()
	remote = {}
	stream = (stream passed in to constructor)
	instance = (wrapper if object, or new wrapper())
	id = (random id)
	
conn inherits from EventEmitter and emits connect, end, and data messages.
*/
exports['test conn'] = function(test){

new Conn(stream,wrapper,opts)

test.finish();
}


